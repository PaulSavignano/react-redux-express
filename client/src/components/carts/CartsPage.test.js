import React from 'react'
import ReactDOM from 'react-dom'
import TestUtils from 'react-addons-test-utils'
import expect from 'expect'

import CartsPage from './CartsPage'

describe('CartsPage', () => {
  it('should exist', () => {
    expect(CartsPage).toExist()
  })
})
